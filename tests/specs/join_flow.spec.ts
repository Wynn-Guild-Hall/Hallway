// Covers the full user journey: homepage renders, /join username lookup
// happy-path, and role-toggle updates the `hall request N` code display live.

import { test } from "@playwright/test";

test.skip("homepage renders the Guild Hall intro", async ({ page }) => {
  // stub
});

test.skip("/join lookup happy-path shows the role picker", async ({ page }) => {
  // stub
});

test.skip("role toggles update the live `hall request N` code", async ({ page }) => {
  // stub
});
